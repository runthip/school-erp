-- ==================================================================
--  48_must_change_password.sql — บังคับเปลี่ยนรหัสผ่านเมื่อแอดมินรีเซตเป็นค่าเริ่มต้น
--  แอดมินรีเซตรหัสผ่านผู้ใช้ที่ลืมรหัส → ตั้งเป็น "รหัสผ่านเริ่มต้น" + ตั้งธงนี้
--  ผู้ใช้ล็อกอินด้วยรหัสเริ่มต้นแล้วถูกบังคับให้ตั้งรหัสใหม่ทันที (ธงถูกล้าง)
--  นำเข้าหลัง 01-47 · รันซ้ำได้ปลอดภัย
-- ==================================================================
USE school_erp;
SET NAMES utf8mb4;

ALTER TABLE users
  ADD COLUMN IF NOT EXISTS must_change_password TINYINT(1) NOT NULL DEFAULT 0 AFTER password_changed_at;

-- จบ 48
