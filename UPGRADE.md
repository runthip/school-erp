# คู่มืออัปเดตและแก้ไขระบบ — School ERP

> เอกสารนี้ตอบคำถาม: **“จะอัปเดต/แก้ระบบอย่างไรให้ไม่กระทบการทำงาน และใช้ได้ครบทุกโรงเรียน”**
> คู่มือใช้งานทั่วไปดูที่ [MANUAL.md](MANUAL.md) · การติดตั้งครั้งแรกดูที่ [DEPLOY.md](DEPLOY.md)

---

## สารบัญ

1. [หลักการ 6 ข้อ](#1-หลักการ-6-ข้อ)
2. [อัปเดตด้วยคำสั่งเดียว](#2-อัปเดตด้วยคำสั่งเดียว)
3. [ขั้นตอนละเอียด (ทำมืออย่างปลอดภัย)](#3-ขั้นตอนละเอียด)
4. [กติกาการแก้โค้ด/ฐานข้อมูล ให้ของเดิมไม่พัง](#4-กติกาการแก้โค้ดฐานข้อมูล)
5. [ทดสอบบนเครื่องทดลองก่อนขึ้นจริง](#5-ทดสอบบนเครื่องทดลองก่อนขึ้นจริง)
6. [ทำให้ครบทุกโรงเรียน](#6-ทำให้ครบทุกโรงเรียน)
7. [ย้อนกลับเมื่อมีปัญหา (Rollback)](#7-ย้อนกลับเมื่อมีปัญหา)
8. [ควรอัปเดตตอนไหน · แจ้งผู้ใช้อย่างไร](#8-ควรอัปเดตตอนไหน)
9. [ปัญหาที่พบบ่อยตอนอัปเดต](#9-ปัญหาที่พบบ่อยตอนอัปเดต)
10. [ใบตรวจก่อน-หลังอัปเดต](#10-ใบตรวจก่อน-หลังอัปเดต)

---

## 1. หลักการ 6 ข้อ

| # | หลักการ | เหตุผล |
|---|---|---|
| 1 | **เพิ่ม อย่าแก้ของเดิม** | เพิ่มตาราง/คอลัมน์/เมนูใหม่ปลอดภัยเสมอ · การเปลี่ยนชื่อหรือลบของเดิมทำให้หน้าที่ใช้อยู่พังทันที |
| 2 | **ทุกการแก้ฐานข้อมูลต้องเป็นไฟล์ `database/NN_ชื่อ.sql`** | ระบบจดว่าโรงเรียนไหนนำเข้าไฟล์ไหนแล้ว → อัปเดตครบทุกโรงเรียนโดยไม่ซ้ำซ้อน |
| 3 | **ไฟล์ SQL ต้องรันซ้ำได้** (`IF NOT EXISTS`, `INSERT IGNORE`) | รันซ้ำโดยไม่ตั้งใจแล้วข้อมูลไม่พัง — เป็นตาข่ายนิรภัยชั้นสุดท้าย |
| 4 | **สำรองก่อนเสมอ** | ไฟล์สำรอง 1 ไฟล์ = ย้อนกลับได้ใน 5 นาที ถ้าไม่มี = แก้ไม่ได้เลย |
| 5 | **ปิดปรับปรุงระหว่างแก้โครงสร้าง** | กันผู้ใช้บันทึกข้อมูลตอนตารางกำลังเปลี่ยน ซึ่งทำให้ข้อมูลค้างครึ่ง ๆ กลาง ๆ |
| 6 | **ทดสอบบนสำเนาก่อนขึ้นจริง** | ปัญหาที่เจอบนสำเนา = ไม่กระทบใคร · ปัญหาที่เจอบนของจริง = ทั้งโรงเรียนหยุดทำงาน |

---

## 2. อัปเดตด้วยคำสั่งเดียว

**ขั้นที่ 1** — อัปโหลด/ดึงโค้ดชุดใหม่ทับของเดิม โดย **ห้ามทับ** 4 อย่างนี้:
`.env` · `storage/` · `public/uploads/` · `backups/`

```bash
rsync -av --delete \
  --exclude='.env' --exclude='storage/' --exclude='public/uploads/' --exclude='backups/' \
  ./school-erp-ใหม่/ /var/www/school-erp/
```

**ขั้นที่ 2** — ดูก่อนว่าจะเกิดอะไรขึ้น (ไม่แตะระบบเลย)

```bash
bash deploy/update.sh --dry-run
```

**ขั้นที่ 3** — อัปเดตจริง

```bash
bash deploy/update.sh --url=https://erp.โรงเรียนของคุณ
```

สคริปต์ทำให้ครบ 6 ขั้น: ตรวจสิ่งที่ค้าง → **ปิดปรับปรุง** → **สำรองทุกโรงเรียน** → **นำเข้า SQL ให้ทุกโรงเรียน** → ตรวจโครงสร้าง + ทดสอบเว็บ → **เปิดใช้งาน**

> ถ้ามีขั้นใดล้มเหลว ระบบจะ **คงโหมดปิดปรับปรุงไว้** พร้อมบอกวิธีแก้ — ผู้ใช้จะไม่เจอระบบครึ่ง ๆ กลาง ๆ

---

## 3. ขั้นตอนละเอียด

ถ้าอยากคุมเองทีละขั้น (หรือสคริปต์ใช้ไม่ได้บนเซิร์ฟเวอร์นั้น)

```bash
cd /var/www/school-erp

# 1) ดูว่าค้างอะไรอยู่บ้าง ทุกโรงเรียน
php deploy/migrate.php --status

# 2) ปิดปรับปรุง (สคริปต์จะแจ้งกุญแจสำหรับผู้ดูแลไว้เข้าทดสอบ)
bash deploy/maintenance.sh on

# 3) สำรองฐานข้อมูลทุกโรงเรียน + ไฟล์แนบ
bash deploy/backup.sh

# 4) อัปโหลดโค้ดใหม่ (ถ้ายังไม่ได้ทำ) — อย่าทับ .env, storage/, public/uploads/, backups/

# 5) นำเข้าไฟล์ SQL ที่ค้าง ให้ทุกโรงเรียน
php deploy/migrate.php

# 6) ตรวจว่าโครงสร้างครบตามที่ทุกโมดูลต้องใช้
php deploy/migrate.php --check

# 7) เข้าเว็บด้วยกุญแจผู้ดูแล ทดสอบหน้าที่แก้ไป
#    https://เว็บของคุณ/?key=<กุญแจจากขั้นที่ 2>

# 8) เปิดใช้งานตามปกติ
bash deploy/maintenance.sh off
```

**คำสั่งที่มีให้ใช้**

| คำสั่ง | ทำอะไร |
|---|---|
| `php deploy/migrate.php --status` | ดูไฟล์ที่ค้างของทุกโรงเรียน (ไม่แก้อะไร) |
| `php deploy/migrate.php --dry-run` | เหมือน `--status` |
| `php deploy/migrate.php` | นำเข้าไฟล์ที่ค้าง **ทุกโรงเรียน** |
| `php deploy/migrate.php --school=10123456` | เฉพาะโรงเรียนเดียว |
| `php deploy/migrate.php --only=58_xxx.sql` | เฉพาะไฟล์เดียว |
| `php deploy/migrate.php --check` | ตรวจว่าตาราง/คอลัมน์ที่โมดูลต้องใช้ครบไหม |
| `bash deploy/maintenance.sh on/off/status` | เปิด/ปิด/ดูสถานะโหมดปรับปรุง |
| `bash deploy/backup.sh` | สำรอง DB ทุกโรงเรียน + ไฟล์แนบ (เก็บ 30 วัน) |

> 💡 ผู้ดูแลของโรงเรียนที่ไม่ถนัดบรรทัดคำสั่ง ใช้เมนู **ความปลอดภัย → นำเข้าฐานข้อมูล** ในระบบแทนได้
> (ทำได้เฉพาะโรงเรียนของตัวเอง — ผู้ให้บริการหลายโรงเรียนควรใช้ `migrate.php` เพื่อทำครบทุกแห่งในครั้งเดียว)

---

## 4. กติกาการแก้โค้ด/ฐานข้อมูล

### 4.1 เมื่อต้องเปลี่ยนโครงสร้างฐานข้อมูล

สร้างไฟล์ใหม่ `database/58_ชื่องาน.sql` (เลขต่อจากไฟล์ล่าสุด **ห้ามแก้ไฟล์เก่าที่ปล่อยไปแล้ว**)

```sql
-- 58_ชื่องาน.sql — คำอธิบายสั้น ๆ ว่าไฟล์นี้ทำอะไร
-- รันซ้ำได้ปลอดภัย

-- เพิ่มตารางใหม่
CREATE TABLE IF NOT EXISTS my_table (
  id BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
  ...
  PRIMARY KEY (id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

-- เพิ่มคอลัมน์ในตารางเดิม
ALTER TABLE students ADD COLUMN IF NOT EXISTS my_field VARCHAR(100) NULL;

-- เพิ่มสิทธิ์ใหม่ + ผูกกับบทบาท
INSERT IGNORE INTO permissions (code, name, module) VALUES ('my.perm','ชื่อสิทธิ์ภาษาไทย','general');
INSERT IGNORE INTO role_permissions (role_id, permission_id)
SELECT r.id, p.id FROM roles r, permissions p
WHERE r.code IN ('head_general','clerk') AND p.code='my.perm';
```

**ต้องทำ**

- ✅ `CREATE TABLE IF NOT EXISTS` · `ADD COLUMN IF NOT EXISTS` · `INSERT IGNORE` / `ON DUPLICATE KEY UPDATE`
- ✅ คอลัมน์ใหม่ในตารางเดิม ต้อง `NULL` ได้ หรือมี `DEFAULT` — ไม่งั้นแถวเดิมนำเข้าไม่ผ่าน
- ✅ ทดสอบรันไฟล์ **2 ครั้งซ้อน** ต้องไม่ error และข้อมูลไม่ซ้ำ

**ห้ามทำ**

- ❌ ลบ/เปลี่ยนชื่อคอลัมน์ที่โค้ดเดิมยังใช้อยู่ → ถ้าจำเป็นให้ทำ **2 รอบ**: รอบนี้เพิ่มของใหม่ + เลิกใช้ของเก่า, รอบถัดไป (หลังทุกโรงเรียนอัปเดตแล้ว) จึงลบ
- ❌ `DELETE`/`UPDATE` ข้อมูลจริงในไฟล์ migration โดยไม่มีเงื่อนไขชัดเจน
- ❌ แก้ไฟล์ `database/NN_*.sql` ที่เคยปล่อยออกไปแล้ว — โรงเรียนที่นำเข้าไปแล้วจะไม่ได้รับการแก้นั้น

### 4.2 เมื่อเพิ่มหน้า/โมดูลใหม่

| ต้องแตะไฟล์ | ทำอะไร |
|---|---|
| `database/NN_*.sql` | ตาราง + สิทธิ์ใหม่ |
| `app/Models/Xxx.php` | คิวรีข้อมูล |
| `app/Controllers/XxxController.php` | ตรวจสิทธิ์ด้วย `$this->authorize('perm')` และ CSRF ด้วย `$this->verifyCsrf()` **ทุกครั้ง** |
| `config/routes.php` | ผูก URL (อย่าลืมใส่ `$auth` middleware) |
| `config/menu.php` | เพิ่มเมนู + ชื่อสิทธิ์ภาษาไทย + ผูกบทบาท |
| `app/Views/xxx/*.php` | หน้าจอ (ซ่อนปุ่มตามสิทธิ์ให้ตรงกับที่ controller ตรวจ) |
| `app/Core/Schema.php` | ถ้าโมดูลต้องใช้ตารางใหม่ ให้เพิ่มใน `REQUIREMENTS` เพื่อให้ `--check` ตรวจให้ |

### 4.3 ข้อควรระวังเฉพาะระบบนี้ (เจ็บมาแล้วจริง)

| เรื่อง | กติกา |
|---|---|
| **โฟลเดอร์อัปโหลดใหม่** | ต้อง **สร้างล่วงหน้า** และ `chmod 777` — เว็บเซิร์ฟเวอร์สร้างโฟลเดอร์ใต้ `storage/` เองไม่ได้ → อัปโหลดจะล้มเหลวเงียบ ๆ (`update.sh` สร้างให้อัตโนมัติ ถ้าเพิ่มโฟลเดอร์ใหม่ให้เติมชื่อในสคริปต์ด้วย) |
| **ไฟล์ `.env`** | ต้องให้ผู้ใช้ที่รันเว็บ **อ่านได้** (`chmod 644` หรือ `chown www-data && chmod 640`) ถ้าอ่านไม่ได้ ระบบจะเงียบ ๆ ถอยไปใช้ค่า default → ต่อฐานข้อมูลผิดตัว อาการคือ “ล็อกอินไม่ผ่านทั้งที่รหัสถูก” |
| **ไลบรารีหน้าเว็บ** | ห้ามใส่ลิงก์ CDN — โรงเรียนใช้งานออฟไลน์ ต้องวางไฟล์ไว้ใน `public/assets/` เท่านั้น |
| **JS ต่อหน้า** | `$GLOBALS['page_scripts']` เป็น string เดี่ยว ต้องต่อสตริงแบบ `'.base_url('assets/js/x.js').'` (ใช้ `<?= ?>` ไม่ได้) |
| **เซสชัน** | อย่ารัน `php -S` คู่กับ Apache บนเครื่องเดียวกัน — ไฟล์เซสชันคนละเจ้าของ ทำให้ขึ้น “CSRF ไม่ถูกต้อง” |
| **ตรวจไวยากรณ์ก่อนอัปโหลดเสมอ** | `find app config public -name '*.php' -exec php -l {} \; \| grep -v "No syntax errors"` |

### 4.4 เลขรุ่น

แก้ไฟล์ `VERSION` ทุกครั้งที่ปล่อยของใหม่ (เช่น `1.1.0`) — `update.sh` จะแสดงให้เห็นว่ากำลังอัปเดตจากรุ่นไหน
และควรจดสรุปสิ่งที่เปลี่ยนไว้ (เช่นในไฟล์ `CHANGELOG.md` หรือหมายเหตุการปล่อยของท่านเอง)

---

## 5. ทดสอบบนเครื่องทดลองก่อนขึ้นจริง

**วิธีที่ปลอดภัยที่สุด: ทดสอบกับสำเนาข้อมูลจริง**

```bash
# 1) ทำสำเนาฐานข้อมูลของโรงเรียนหนึ่งมาเป็นเครื่องทดลอง
mysql -u root -p -e "CREATE DATABASE erp_staging CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;"
gunzip < backups/db_erp_10123456_20260728_020000.sql.gz | mysql -u root -p erp_staging

# 2) วางโค้ดชุดใหม่ไว้อีกโฟลเดอร์ แล้วทำ .env ของตัวเองชี้ไป erp_staging
cp -r /var/www/school-erp /var/www/erp-staging
cd /var/www/erp-staging
sed -i 's/^DB_NAME=.*/DB_NAME=erp_staging/; s/^MULTI_TENANT=.*/MULTI_TENANT=false/' .env

# 3) ลองอัปเดตกับสำเนา
php deploy/migrate.php --status
php deploy/migrate.php
php deploy/migrate.php --check
```

จากนั้นเปิดเว็บของ staging แล้วลอง **งานที่ใช้บ่อยและงานที่เพิ่งแก้**: เข้าสู่ระบบ · เช็คชื่อ · บันทึกคะแนน · พิมพ์ ปพ. · สารบรรณ · งป.01
ผ่านครบแล้วค่อยทำกับของจริง

---

## 6. ทำให้ครบทุกโรงเรียน

### กรณี ก — ระบบหลายโรงเรียน (`MULTI_TENANT=true`) ★ อัปเดตครั้งเดียวได้ทุกแห่ง

- โค้ดมีชุดเดียว → อัปโหลดโค้ดใหม่ครั้งเดียว ทุกโรงเรียนได้พร้อมกัน
- `php deploy/migrate.php` จะ **ไล่ทำทีละฐานข้อมูลตามทะเบียนกลาง** รวมถึงโรงเรียนที่ถูกระงับ (เพื่อให้พร้อมเมื่อเปิดใช้อีกครั้ง)
- `bash deploy/backup.sh` สำรอง **ฐานข้อมูลกลาง + ทุกโรงเรียน** ให้อัตโนมัติ
- **โรงเรียนที่เปิดใหม่หลังจากนี้** ระบบจะนำเข้าไฟล์ที่ใหม่กว่าชุดติดตั้งให้เองตอนกด “เปิดใช้งานโรงเรียนใหม่” จึงได้รุ่นล่าสุดเท่ากับโรงเรียนอื่นเสมอ

**ถ้าเพิ่มไฟล์ migration ใหม่ ควรอัปเดตชุดติดตั้งด้วย** (ทำให้โรงเรียนใหม่ติดตั้งเร็วขึ้น ไม่บังคับ)

```bash
# สร้าง deploy/school_erp_clean.sql ใหม่จากฐานข้อมูลที่โครงสร้างล่าสุดและไม่มีข้อมูลองค์กร
mysqldump -u root -p --no-data erp_ตัวอย่างที่สะอาด > /tmp/structure.sql
# แล้วรวมกับ INSERT ค่าตั้งต้น + รายการ schema_migrations ทั้งหมด (ดูรูปแบบในไฟล์เดิม)
```

### กรณี ข — แยกโค้ดคนละชุด/คนละโดเมนต่อโรงเรียน

ต้องทำซ้ำทุกโฟลเดอร์ ใช้ลูปช่วยได้

```bash
for d in /var/www/erp-school1 /var/www/erp-school2 /var/www/erp-school3; do
  echo "===== $d ====="
  rsync -av --delete --exclude='.env' --exclude='storage/' \
        --exclude='public/uploads/' --exclude='backups/' ./school-erp-ใหม่/ "$d/"
  ( cd "$d" && bash deploy/update.sh )
done
```

---

## 7. ย้อนกลับเมื่อมีปัญหา

> เงื่อนไขเดียวที่ทำให้ย้อนกลับได้: **มีไฟล์สำรองก่อนอัปเดต** — `update.sh` ทำให้อัตโนมัติทุกครั้ง

```bash
# 1) ปิดระบบก่อน กันไม่ให้มีข้อมูลใหม่เข้ามาระหว่างกู้คืน
bash deploy/maintenance.sh on

# 2) กู้ฐานข้อมูลของโรงเรียนที่มีปัญหา (ใช้ไฟล์ล่าสุดก่อนอัปเดต)
gunzip < backups/db_erp_10123456_20260728_020000.sql.gz | mysql -u USER -p erp_10123456

# 3) คืนโค้ดชุดเดิม (สำเนาที่เก็บไว้ หรือ git checkout รุ่นก่อนหน้า)
#    ถ้าใช้ git:  git checkout v1.0.0

# 4) ตรวจแล้วเปิดระบบ
php deploy/migrate.php --check
bash deploy/maintenance.sh off
```

**หลักคิดสำคัญ**: ถ้าโค้ดใหม่ทำงานผิดแต่ฐานข้อมูลยังดี ให้ **ย้อนแค่โค้ด** ก่อน (เร็วและปลอดภัยกว่า)
ย้อนฐานข้อมูลเมื่อจำเป็นจริง ๆ เท่านั้น เพราะข้อมูลที่ผู้ใช้บันทึกหลังจากจุดสำรองจะหายไป

**เก็บโค้ดรุ่นก่อนไว้เสมอ** ก่อนอัปโหลดทับ:

```bash
tar -czf backups/code_$(date +%Y%m%d).tar.gz \
    --exclude='storage' --exclude='backups' --exclude='public/uploads' -C /var/www school-erp
```

---

## 8. ควรอัปเดตตอนไหน

| ระดับการเปลี่ยนแปลง | เวลาที่เหมาะ | ต้องปิดปรับปรุงไหม |
|---|---|---|
| แก้ข้อความ/หน้าตา/รายงาน (ไม่แตะฐานข้อมูล) | เวลาใดก็ได้ที่คนใช้น้อย | ไม่ต้อง (อัปโหลดทับได้เลย) |
| เพิ่มตาราง/คอลัมน์ใหม่ | นอกเวลาราชการ หรือช่วงพักกลางวัน | **ต้อง** (ไม่กี่นาที) |
| เปลี่ยนโครงสร้างใหญ่ / ย้ายข้อมูล | วันหยุด หรือช่วงปิดภาคเรียน | **ต้อง** |
| อัปเดตด่วนเพราะระบบมีบั๊ก | ทันที แต่สำรองก่อนเสมอ | แล้วแต่ขนาด |

**เลี่ยงช่วงเวลาต้องห้าม**: ช่วงกรอกคะแนนปลายภาค · วันปิดคะแนน · ช่วงพิมพ์ ปพ. · วันสิ้นปีงบประมาณ (30 ก.ย.)

**แจ้งผู้ใช้ล่วงหน้า** อย่างน้อย 1 วัน (เช่น ประกาศในปฏิทินวิชาการ หรือหนังสือเวียนในระบบสารบรรณ) ระบุ:
วันเวลาที่ปิด · ระยะเวลาโดยประมาณ · สิ่งที่เปลี่ยนแปลง · ย้ำว่าข้อมูลไม่หาย

---

## 9. ปัญหาที่พบบ่อยตอนอัปเดต

| อาการ | สาเหตุ / วิธีแก้ |
|---|---|
| `--status` บอกว่าค้างหลายไฟล์ ทั้งที่ระบบใช้งานได้ปกติ | เคยนำเข้า SQL เองด้วย phpMyAdmin/mysql โดยระบบไม่ได้จด → รัน `php deploy/migrate.php --check` ถ้าโครงสร้างครบ ให้ปล่อยให้รันทับ (ไฟล์รันซ้ำได้) หรือกด **“ทำเครื่องหมายว่านำเข้าแล้ว”** ที่หน้า *นำเข้าฐานข้อมูล* |
| ขึ้นว่า “ไฟล์ … รันซ้ำอัตโนมัติไม่ได้” | เป็นไฟล์ติดตั้งเริ่มต้น (เช่น `01_schema.sql`) ที่ระบบกันไว้ไม่ให้รันทับฐานข้อมูลที่มีข้อมูลแล้ว → ทำเครื่องหมายว่านำเข้าแล้วในหน้าเดียวกัน |
| หลังอัปเดตเข้าเว็บแล้ว 500 | ดู error log ของ Apache และ `storage/logs/` · มักเป็นไฟล์อัปโหลดไม่ครบ หรือ PHP ต่างรุ่น |
| ล็อกอินไม่ผ่านทั้งที่รหัสถูก | เว็บเซิร์ฟเวอร์อ่าน `.env` ไม่ได้ → `chmod 644 .env` |
| อัปโหลดไฟล์ไม่สำเร็จเฉพาะโมดูลใหม่ | ยังไม่ได้สร้างโฟลเดอร์ปลายทาง → `mkdir -p storage/<ชื่อ> && chmod 777 storage/<ชื่อ>` |
| หน้าเว็บไม่มีสไตล์หลังอัปเดต | อัปโหลด `public/assets/` ไม่ครบ · ให้ผู้ใช้กด `Ctrl+Shift+R` ล้างแคช |
| ผู้ใช้ยังเห็นหน้า “ระบบกำลังปรับปรุง” ทั้งที่อัปเดตเสร็จแล้ว | ยังไม่ได้ปิดโหมด → `bash deploy/maintenance.sh off` |
| โรงเรียนหนึ่งอัปเดตไม่ผ่าน แต่ที่เหลือผ่าน | สคริปต์รายงานเฉพาะแห่งที่ล้ม → แก้แล้วรันเฉพาะแห่งนั้น `php deploy/migrate.php --school=<รหัส>` |

---

## 10. ใบตรวจก่อน-หลังอัปเดต

**ก่อนอัปเดต**

- [ ] ทดสอบบนสำเนา (staging) ผ่านแล้ว
- [ ] `php -l` ผ่านทุกไฟล์ที่แก้
- [ ] ไฟล์ SQL ใหม่รันซ้ำได้ (ทดลองรัน 2 ครั้ง)
- [ ] อัปเดตเลขรุ่นในไฟล์ `VERSION`
- [ ] แจ้งผู้ใช้ล่วงหน้าแล้ว · เลือกเวลาที่คนใช้น้อย
- [ ] เก็บสำเนาโค้ดรุ่นเดิมไว้แล้ว
- [ ] `bash deploy/update.sh --dry-run` ดูผลแล้วเป็นไปตามที่คาด

**หลังอัปเดต**

- [ ] `php deploy/migrate.php --check` ผ่านทุกโรงเรียน
- [ ] เข้าสู่ระบบได้ (ลองอย่างน้อย 2 บทบาท เช่น แอดมิน + ครู)
- [ ] เมนูใหม่ปรากฏกับบทบาทที่ควรเห็น และ **ไม่ปรากฏ** กับบทบาทที่ไม่ควรเห็น
- [ ] ทดสอบงานที่ใช้บ่อย: เช็คชื่อ · บันทึกคะแนน · พิมพ์เอกสาร 1 ใบ · สารบรรณ 1 ฉบับ
- [ ] ทดสอบอัปโหลดไฟล์ 1 ไฟล์ (ตรวจสิทธิ์โฟลเดอร์)
- [ ] ปิดโหมดปรับปรุงแล้ว (`bash deploy/maintenance.sh status` ต้องขึ้น “เปิดใช้งานตามปกติ”)
- [ ] มีไฟล์สำรองก่อนอัปเดตเก็บไว้ใน `backups/`

---

### ไฟล์/สคริปต์ที่เกี่ยวข้อง

| ไฟล์ | หน้าที่ |
|---|---|
| `deploy/update.sh` | อัปเดตครบวงจร (ปิดปรับปรุง → สำรอง → นำเข้า → ตรวจ → เปิด) |
| `deploy/migrate.php` | นำเข้าไฟล์ SQL ที่ค้าง **ทุกโรงเรียน** · `--status` `--check` `--school=` `--only=` |
| `deploy/maintenance.sh` | เปิด/ปิดโหมดปิดปรับปรุง พร้อมกุญแจให้ผู้ดูแลเข้าทดสอบ |
| `deploy/backup.sh` | สำรองฐานข้อมูล (ทุกโรงเรียน) + ไฟล์แนบ |
| `database/NN_*.sql` | ไฟล์ปรับปรุงฐานข้อมูล เรียงตามลำดับ รันซ้ำได้ |
| `VERSION` | เลขรุ่นของโค้ดชุดนี้ |
| [DEPLOY.md](DEPLOY.md) · [MANUAL.md](MANUAL.md) | ติดตั้งครั้งแรก · คู่มือใช้งาน |
